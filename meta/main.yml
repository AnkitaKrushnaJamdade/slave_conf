galaxy_info:
  author: Ankita Jamdade
  description: this role will configure k8s slave for you

  min_ansible_version: 2.9

  galaxy_tags: 
   - slave configuration
   - kubernetes
   
dependencies: boto library master-instance master_conf slave-instance roles

  
  
